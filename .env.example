# Frontend env (Vite). Copy to .env.local for local overrides.
#
# --- 方式 A（推荐）：Vercel rewrite 把 /api 代理到后端，此项留空 ---
# VITE_API_BASE_URL=
#
# --- 方式 B：前后端不同域名，浏览器直连后端（需后端配置 CORS + Cookie SameSite=none）---
# VITE_API_BASE_URL=https://your-backend.vercel.app
